function main ()
 var user
 read user
 var local = (3 + user)

 call res = 'foo (user)
 print res
 print local

function foo (var choice)
 branch (choice==0) $la $lb
 $la:
  return 1
 $lb:
  return 0
